platform :ios, '9.0'

target 'register_login_template' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for register_login_template
    pod 'Firebase/Core'
    pod 'Firebase/Auth'

end
